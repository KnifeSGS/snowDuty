import { InitialJournalData } from './initial-journal-data';

describe('InitialJournalData', () => {
  it('should create an instance', () => {
    expect(new InitialJournalData()).toBeTruthy();
  });
});
